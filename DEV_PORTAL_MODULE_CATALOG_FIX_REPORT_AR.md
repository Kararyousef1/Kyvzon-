# تقرير إصلاح عرض جميع البوابات في نموذج الشركة — بوابة المطور

تاريخ التنفيذ: 2026-07-16

## المشكلة

في صفحة بوابة المطور عند إضافة أو تعديل شركة، كان نموذج تفعيل البوابات يعرض 6 بوابات فقط:

- بوابة الموظف
- الموارد البشرية
- الإدارة
- الحركة/الحراسة
- التواصل
- البوابة التقنية

بينما كتالوج النظام الحقيقي يحتوي 14 بوابة/وحدة.

## السبب

السبب أن `CompaniesPage.tsx` كانت تستخدم قائمة hardcoded قديمة داخل نموذج الشركة بدل الاعتماد على كتالوج البوابات الرسمي:

```text
MODULE_CATALOG
```

الموجود في:

```text
src/services/sdk/TenantModuleCatalog.ts
```

## الإصلاح

تم تعديل:

```text
src/pages/devportal/pages/CompaniesPage.tsx
```

ليستخدم:

```ts
MODULE_CATALOG
modulesForPlan
```

بدل القائمة اليدوية.

## البوابات التي تظهر الآن

الآن يظهر كامل كتالوج النظام:

1. بوابة الموظف
2. بوابة الموارد البشرية
3. بوابة الإدارة
4. بوابة المدير
5. بوابة المشرف
6. بوابة الحراسة
7. بوابة الحركة
8. بوابة التواصل
9. البوابة التقنية
10. الذكاء الاصطناعي
11. التقارير المتقدمة
12. الصحة والسلامة
13. تخطيط التعاقب
14. عقود الموظفين

## تحسين إضافي

أصبحت كل بوابة تعرض:

- الاسم.
- الوصف.
- التصنيف.
- هل هي ضمن الخطة الحالية أم خارج الخطة.

وهذا مهم لأن بوابة المطور هي Control Plane، ويمكنها تفعيل بوابات مخصصة حتى لو كانت خارج الخطة القياسية عند الحاجة.

## التحقق

تم تشغيل:

```bash
npm run type-check
npm run check:all
npm audit --audit-level=moderate
```

النتيجة:

```text
TypeScript type-check: PASS
SDK Boundary Check: PASS
DB Contract Check: PASS
Tests: 251 passed
Test Files: 16 passed
Production Build: PASS
npm audit: 0 vulnerabilities
```
