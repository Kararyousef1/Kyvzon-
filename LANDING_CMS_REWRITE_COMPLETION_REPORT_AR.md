# تقرير إعادة كتابة صفحة إدارة صفحة الزوار — Kyvzon Developer Portal

تاريخ التنفيذ: 2026-07-16

## الهدف

إعادة بناء صفحة "إدارة صفحة الزوار" داخل بوابة المطور لتصبح متوافقة مع التغييرات الجديدة في صفحة الهبوط والموقع العام، بدل التصميم القديم الذي كان يحتوي عناصر غير منظمة وغير مناسبة للتجربة الحالية.

---

## ما تم تنفيذه

تمت إعادة كتابة الملف بالكامل:

```text
src/pages/admin/AdminLandingPageCMS.tsx
```

وأصبحت الصفحة الآن لوحة تحكم حديثة ومنظمة بدل الصفحة القديمة.

---

## الأقسام الجديدة داخل الصفحة

### 1. الهوية

تتحكم في:

- اسم الشعار بالعربية.
- اسم الشعار بالإنجليزية.
- رمز الشعار.
- رابط صورة الشعار.
- اللون الرئيسي للموقع.
- معاينة مباشرة للهوية البصرية.

---

### 2. Hero و CTA

تتحكم في:

- عنوان Hero بالعربية.
- عنوان Hero بالإنجليزية.
- وصف Hero بالعربية.
- وصف Hero بالإنجليزية.
- نص CTA الأساسي.
- رابط CTA الأساسي.

---

### 3. الأقسام

تتحكم في إظهار وإخفاء أقسام صفحة الزوار مثل:

- قسم التسويق والرؤية.
- قسم الفيديو.
- قسم الموقع.
- قسم الإحصائيات.
- قسم العناية.
- قسم الوكلاء.

---

### 4. البوابات

تعرض البوابات العامة المتاحة للزوار، وتسمح بإظهار أو إخفاء كل بوابة.

مهم: بوابة المطور لا تظهر هنا لأنها داخلية خاصة بفريق Kyvzon.

---

### 5. الموقع العام

تتحكم في:

- تفعيل التسجيل العام.
- تفعيل مستكشف البوابات.
- بريد الدعم.
- هاتف الدعم.
- صفحات الفوتر العامة.
- عناوين صفحات الفوتر.
- روابط CTA لكل صفحة.

---

### 6. المعاينة

توفر:

- معاينة سريعة لمحتوى Hero المحفوظ.
- معاينة لصفحة الهبوط داخل إطار.
- زر معاينة حية يفتح الصفحة الرئيسية في تبويب جديد.

---

## أين يتم الحفظ؟

### إعدادات صفحة الهبوط الأساسية

تحفظ في:

```text
system_settings.landing_config
```

عبر:

```text
settingsService.updateLandingConfig
```

### إعدادات الموقع العام وصفحات الفوتر

تحفظ في:

```text
system_settings.general_settings.public_site_config
```

عبر:

```text
publicSiteConfigService.updateConfig
```

---

## الملفات المعدلة

```text
src/pages/admin/AdminLandingPageCMS.tsx
```

واعتمدت الصفحة على الخدمات الموجودة مسبقًا:

```text
src/services/sdk/PublicSiteConfigService.ts
src/services/sdk/SettingsService.ts
```

---

## ملاحظات مهمة

1. الصفحة القديمة كانت تحتوي مفاهيم قديمة مثل المنتجات والفيديوهات بشكل غير متوافق مع صفحة الهبوط الجديدة.
2. الصفحة الجديدة أصبحت مرتبة حسب منطق SaaS Landing CMS:
   - الهوية.
   - Hero/CTA.
   - الأقسام.
   - البوابات.
   - صفحات الموقع العام.
   - المعاينة.
3. بوابة المطور أزيلت من الظهور للزوار، لكنها باقية في النظام الداخلي.
4. بعض إعدادات `landing_config` ما زالت تحتاج لاحقًا إلى ربط أعمق داخل مكونات صفحة الهبوط نفسها إذا أردنا أن كل نص في الصفحة يصبح Dynamic بالكامل بدل كونه static من `data.ts` و `i18n.ts`.

---

## التحقق الفني

تم تشغيل:

```bash
npm run type-check
npm run check:all
npm audit --audit-level=moderate
```

النتيجة:

```text
TypeScript type-check: PASS
SDK Boundary Check: PASS
DB Contract Check: PASS
Tests: 251 passed
Test Files: 16 passed
Production Build: PASS
npm audit: 0 vulnerabilities
```

تحذير غير حرج أثناء البناء:

```text
INEFFECTIVE_DYNAMIC_IMPORT
```

وهو تحذير bundling فقط ولا يمنع البناء.
