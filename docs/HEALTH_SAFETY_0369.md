# الصحة والسلامة المهنية (CAPA) — المايجريشن 0369

> المرحلة 4 · بوابة الموارد البشرية · الصفحة الثالثة والعشرون من ست وعشرين
> `src/pages/hr/HealthSafetyPage.tsx` — من **153** سطراً إلى **760**

---

## الخلاصة بالأرقام

| البند | العدد |
|---|---|
| أعطالٌ مُثبتة تشغيلياً قبل كتابة سطر | **14 + 6 في الصفحة** |
| تأكيدات `verify-health-safety-0369.sql` | **44** |
| تأكيدات `-rls.sh` (دور `authenticated`) | **30** |
| أعكاسُ `_invert_0369.py` | **53/53** + مكافئٌ موثَّق |
| تأكيدات `healthSafetyContract.test.ts` | **47** |
| دوالٌ جديدة | **7** |
| قيود CHECK جديدة | **3** |
| مفاتيح أجنبية مركَّبة | **4** |

---

## ★★★★ العطل الأول: ثلاثةٌ من أربعة خياراتٍ تُخرج خطأ

`incidents_category_check` كان يقبل سبعاً:
```
technical · hr · management · workplace · salary · safety · other
```

والصفحة تعرض في `<select>` أربعةً، وتُرشّح بخمسةٍ في `safetyCategories`.

**PROBE_2 — إدراجٌ فعليٌّ بكلٍّ منها:**

```
safety            ⇒ مقبول
health_safety     ⇒ ★ مرفوض (غير موجودٍ في CHECK)
work_injury       ⇒ ★ مرفوض
near_miss         ⇒ ★ مرفوض
security_incident ⇒ ★ مرفوض
```

⇒ **المستخدم يختار «إصابة عمل» فيفشل التسجيل.** و`health_safety`
الخامس في المُرشِّح لا وجود له في القائمة ولا في القاعدة — **شيفرةٌ
ميتة من الطرفين**.

★ العلاج: توسيع CHECK بالثلاثة الحقيقية، وإسقاط `health_safety` من
الصفحة. والسبعة الأصلية باقيةٌ (اختُبر في التأكيد 1.2 — لا انحدار).

---

## أعطال `corrective_actions` — جدولٌ عارٍ إلّا من CHECK اثنين

★ إنصافاً: `incidents` **محميٌّ جيداً** منذ 0338 — سبعة مفاتيح أجنبية
وثلاثة CHECK ومحفّز منع حذف. التناقض أن **إجراءاته التصحيحية تُمحى
بحرّية**.

| # | العطل | الإثبات |
|---|---|---|
| ② | `owner_id` بلا FK | `PROBE_3`: مُسنَدٌ إلى `ffffffff-…` ⇒ قُبِل |
| ③ | ★★★★ إجراءٌ بحادثٍ في مستأجرٍ آخر | `PROBE_4` ⇒ قُبِل |
| ④ | `created_by`/`completed_by` بلا FK | — |
| ⑤ | `title` من مسافات | `PROBE_5` ⇒ قُبِل |
| ⑥ | ★★★ «مكتمل» بلا مُنجِزٍ ولا لحظة | `PROBE_6` ⇒ قُبِل |
| ⑦ | ★★★ «ملغى» بلا سببٍ ولا عمود | `PROBE_7`: أعمدةٌ فيها `cancel` = **0** |
| ⑧ | استحقاقٌ في 2018 | `PROBE_8` ⇒ قُبِل |
| ⑨ | ★★★★ **حادثٌ حرجٌ بلا إجراء — ولا شيء يكشفه** | `PROBE_9`: الفجوة = 1 · دوالُ المنظومة = **0** |
| ⑩ | ★★★ الإجراء المتأخّر لا يُرصد | `PROBE_10`: دوالُ التأخّر = 0 · متأخّرٌ فعلاً = 1 |
| ⑪ | الحذف النهائيّ مسموح | `PROBE_11` ⇒ حُذف صفٌّ |
| ⑫ | حذفُ الحادث يُيتّم إجراءاته | `ON DELETE SET NULL` |
| ⑬ | ★★★ **البوّابة على `admin` لا `hr`** | `PROBE_13` |
| ⑭ | ★★★ **الموظف لا يرى الإجراء المُسنَد إليه** | `PROBE_14`: SELECT تشترط `staff` وحدها |

### ⑥ لماذا هو خطير

سجلُّ CAPA **وثيقةٌ تدقيقية** في نظم السلامة. أن يقول «أُنجز» ولا
يقول من ولا متى — يُفرغه من غرضه كاملاً.

### ⑨ جوهر CAPA

أن كل حادثٍ جسيمٍ يُقابله إجراءٌ تصحيحيّ. لا شيء في المنظومة كان يقيس
هذه الفجوة. الآن `needs_capa` في اللوح و`incidents_no_capa` في الملخّص
— **ويستثنيان الإجراء الملغى** (اختُبر في 5.1b).

### ⑬ الصفحة تنكسر نصفين

`hybrid_gate_incidents` على `hr` و`hybrid_gate_corrective_actions` على
`admin`. مستأجرٌ مشتركٌ في الموارد البشرية دون الإدارة **يرى الحوادث
ولا يرى إجراءاتها**.

---

## ★★★★ عطلٌ في مايجريشني كشفه تأكيدي

أوّل صياغةٍ للمحفّز أفرغت حقول الإلغاء **في INSERT أيضاً**:

```sql
IF NEW.status <> 'cancelled' THEN
  NEW.cancel_reason := NULL;   -- ← يُفرغ في الإدراج والتحديث معاً
END IF;
```

فسقط التأكيد 2.5b بـ«سببُ إلغاءٍ بلا إلغاءٍ قُبِل». الإثبات:

```
INSERT … (title, cancel_reason) VALUES ('سببٌ يتيم','لا شيء');
⇒ status=open · cancel_reason=<NULL>
```

**التناقض يُبتلع صامتاً بدل أن يُرفض بصوتٍ مسموع.** العلاج: الإفراغ
داخل `IF TG_OP = 'UPDATE'` وحده، فيرفضه القيد في الإدراج.

★ العكس `INV09` يُعيد الصياغة الخاطئة ويُثبت أن 2.5b يمسكها.

---

## ★★★ درس التغطية — للمرة الثالثة والثلاثين

أوّل تشغيلٍ لـ`_invert_0369.py` أعطى **45/54**. تسعةٌ نجت:

| # | التشخيص المُثبَت | العلاج |
|---|---|---|
| `INV15`/`INV27` | لا حادثَ **مؤرشفاً** في العيّنة | 5.10 |
| `INV16` | كل إجراءات العيّنة مفتوحة | 5.11 بحادثٍ له إجراءان |
| `INV17` | لا إجراءَ في مستأجرٍ آخر بالمعرّف نفسه | 5.12 بتعطيل FK مؤقتاً |
| `INV18` | ترتيب العيّنة الزمنيّ وافق المرجع صدفةً | 5.13 بحادثٍ أقدمَ لكنه حرجٌ مفتوح |
| `INV22` | لم يُمرَّر `p_status` في تأكيدٍ يعدّ | 5.14 |
| `INV31` | **المحفّز يملأ `created_by` فيُخفي سقوط الدالة** | 5.15 يفحص نصّ الدالة |
| `INV39` | لم يُمرَّر معرّفٌ من مستأجرٍ آخر | 5.16 |
| `POL01` | انظر أدناه | مكافئٌ موثَّق |

★ `INV31` تكرارٌ حرفيٌّ لـ`INV35` في 0368: **المحفّز يُخفي سقوط
الدالة**. صار نمطاً معروفاً — أُدوّنه ليُتوقَّع مستقبلاً.

**النتيجة بعد العلاج: 53/53.**

### `POL01` — مكافئٌ بسببٍ مُثبت

تغييرُ وحدة البوّابة من `hr` إلى `admin` **لا أثر سلوكيّ له**:
`tenants.module_enforcement_mode` افتراضيّه `'off'`، و
`subscription_allows_module()` تُعيد `TRUE` فوراً في هذا الوضع:

```sql
IF v_mode = 'off' THEN RETURN TRUE; END IF;
```

⇒ لا سبيل إلى إثبات العكس بسلوكٍ إلّا بتفعيل الإنفاذ (خارج نطاق
الجولة). **لكنّ الإصلاح ليس زائداً**: متى فُعّل الإنفاذ انكسرت الصفحة
نصفين. الحارس الممكن فحصٌ نصّيّ، وهو مكتوبٌ في التأكيد 6.1.

---

## أخطائي في هذه الجولة

1. **المحفّز يبتلع التناقض في INSERT** (أعلاه) — كشفه تأكيدي 2.5b.
2. **التأكيد 5.6 قاس قيمةً مطلقة** (`capa_overdue = 0`) ناسياً إجراءً
   أنشأته 5.5. عولج بمقارنةٍ مباشرةٍ بالجدول بدل رقمٍ ثابت.

3. **`no-regex-spaces` مجدداً** — مسافتان في نمطٍ واحد. الدرس تكرّر
   للمرة الثالثة (0364 · 0368 · 0369)، لكنّي هذه المرّة **قرأتُ رسالة
   الفشل أولاً** وأصلحتُ بـ`edit_file` على السطر المحدَّد مباشرةً بدل
   سكربتٍ يُخمّن — فتمّ من المحاولة الأولى (مقابل خمس محاولاتٍ في 0368).

---

## الحالة بعد الجولة (مُحقَّقة على قاعدة نظيفة)

```
298 مايجريشن · صفر فشل
77 ملف SQL   · صفر فشل
37 سكربت RLS · صفر فشل
tsc EXIT=0 · test:run 4864/4864 في 163 ملفاً · build 6.92s
contract PASS · lint 0 خطأ
```

---

## الملفات

```
supabase/migrations/0369_health_safety_integrity.sql
tools/dev/verify-health-safety-0369.sql        44 تأكيداً
tools/dev/verify-health-safety-0369-rls.sh     30 تأكيداً
tools/dev/_invert_0369.py                      53/53 + مكافئ
src/services/sdk/OccupationalSafetyService.ts  occupationalSafetySdk
src/pages/hr/HealthSafetyPage.tsx              153 → 760
src/test/healthSafetyContract.test.ts          47 تأكيداً
src/test/portalHygieneContract.test.ts         الحارس المُوحَّد: 11 صفحة
```
