# ماذا تبقّى؟ — جرد مُقاس · حُدِّث 2026-08-08 (جولة 0371)

> كل سطر أدناه **مُستخرَج برمجياً** من الشجرة أو من Postgres محلي مبنيّ
> من **300** مايجريشن. لا شيء منه من الذاكرة.

---

## ما أُنجز — المراحل 0 · 1 · 2 · 3

| المرحلة | الحالة |
|---|---|
| **0** — خريطة الترابط | ✅ `docs/PORTAL_RELATIONS_MAP.md` |
| **1** — النظافة التقنية | ✅ `as any`: 57 → **0** · حارس `portalHygieneContract` |
| **2** — بوابة الموظف صفحة صفحة | ✅ **مكتملة** — آخر ما يلمس Supabase أُغلق في `0344` |
| **3** — فصل البلاغات | ✅ `0341` — شاشتان مستقلّتان |
| **4** — بوابة الموارد صفحة صفحة | ✅ **مكتملة — 26 من 26** — `HRDashboard` ✅ (`0345`) · `hr/AttendancePage` ✅ (`0346`) · `KioskPage` ✅ (`0347`) · `PayrollPage` ✅ (`0348`) · `hr/AnalyticsPage` ✅ (`0349`) · `HRMovementAnalyticsPage` ✅ (`0350`) · `TrainingReportsPage` ✅ (`0351`) · `TrainingManagementPage` ✅ (`0352`) · `TalentMarketPage` ✅ (`0354`) · `LoansPage` ✅ (`0355`) · `PerformancePage` ✅ (`0356`) · `TeamPage` ✅ (`0357`) · `BonusesPage` ✅ (`0358`) · `OnboardingPage` ✅ (`0359`) · `DocumentsPage` ✅ (`0360`) · `SuccessionPlanningPage` ✅ (`0361`) · `RecruitmentPage` ✅ (`0362`) · `ExpensesPage` ✅ (`0363`) · `EmployeeContractsPage` ✅ (`0364`) · `DisciplinaryPage` ✅ (`0365`+`0366`) · `HRServiceCenterPage` ✅ (`0367`) · `ShiftSchedulingPage` ✅ (`0368`) · `HealthSafetyPage` ✅ (`0369`) · `ReportsPage` ✅ (`0370`) · `HRCommunicationPage` ✅ (`0371`) · `DisciplinaryPage` ✅ (`0365`) · `HRServiceCenterPage` ✅ (`0367`) · `ShiftSchedulingPage` ✅ (`0368`) · `HealthSafetyPage` ✅ (`0369`) |
| **5** — الترابط بين البوابات | 🟡 جزئي (`0334` الاعتمادات · `0344` الإجازة↔الحضور) |
| **6** — التصميم والاكتمال | ⬜ لم تبدأ |

**ثلاثة وثلاثون مايجريشناً** في هذه السلسلة: `0332`–`0364`.

### الحالة المُقاسة (قاعدة نظيفة من الصفر)

```
مايجريشنات : 288  · فشل: 0
ملفات SQL  : 66   · فشل: 0
سكربتات RLS: 28   · فشل: 0
tsc EXIT=0 · test:run 4185/4185 في 154 ملفاً · build ✅ 5.55s
contract PASS · lint 0 خطأ (1178 تحذيراً) · boundary صفر في الجديد
allowlist ميّت: 3 (مالية)
```

---

## ★★★ أولاً: ما يمنع كل شيء آخر

### ① ثمان وعشرون مايجريشناً لم تُدفع

```
0332 0333 0334 0335 0336 0337 0338 0339 0340 0341 0342 0343 0344 0345 0346 0347 0348 0349 0350 0351 0352 0353 0354 0355 0356 0357 0358 0359 0360 0361 0362 0363 0364
```

★ بقرارك: «سنقوم بدفع ورفع المايجريشنات عندما ننتهي من هذه البوابتين».

**بلا `db push` كل ما بُني أعلاه غير موجود في قاعدتك.** الشاشات الجديدة
(`MyProblemsPage` · `HrProblemsInboxPage` · `PermissionsPage` ·
`LeaveRequestPage`) تستدعي دوالّ لا وجود لها ⇒ **ستفشل كلها**.

هذا البند **يسبق كل ما بعده**.

### ② المتصفح لم يُختبر إطلاقاً

كل ما أُنجز: منطق قاعدة (273 مايجريشن) · فحص ثابت (3439 اختباراً) ·
جلسات RLS (184 تأكيداً). **صفر اختبار متصفح.**

الشاشات الأربع المُعاد كتابتها لم يفتحها إنسان بعد.

### ③ الفرع متشعّب

```
2 commits محلية مقابل 1 على origin
```

يحتاج `git fetch origin` وقراراً — ولا `--force` بلا مراجعة.

---

## ثانياً: المرحلة 2 — بوابة الموظف (4 من 8)

| # | الصفحة | أسطر | الحالة |
|---|---|---|---|
| 1 | `EmployeeDashboard` | 625 | ✅ `0335` |
| 2 | `ProfilePage` | 716 | ✅ `0336` |
| 3 | `MyAttendancePage` | 551 | ✅ `0337` |
| 4 | `LeaveRequestPage` | 637 | ✅ `0339` |
| 5 | `MyExpensesPage` + `MyLoansPage` | 361 + 306 | ⬜ **متبقٍّ** |
| 6 | `MyPayrollPage` | 316 | ⬜ **متبقٍّ** |
| 7 | `SOPsPage` | **869** | ⬜ **متبقٍّ** — الأكبر · يلمس Supabase |
| 8 | الباقي | — | ⬜ `TrainingPage` 446 · `WellnessPage` 439 · `AIInsightsDashboard` 364 · `ContactPage` 319 · `MyGoalsPage` 315 · `SurveyPage` 236 · `AIChatPage` 227 |

**+ صفحتان خارج الترقيم لُمستا في الجولات الأخيرة:**
`PermissionsPage` (`0340`) · `MyProblemsPage` (`0341`).

**+ صفحتان لم تُلمسا وتخصّان البلاغات:**
`NewProblemPage` (495) · `ProblemDetail` (400) — كلتاهما تلمس Supabase.

---

## ثالثاً: المرحلة 4 — بوابة الموارد البشرية (23 من 26)

**★ تحديث 0369:** ثلاثٌ وعشرون صفحةً أُنجزت. **الصفحتان المتبقّيتان**:

| الصفحة | أسطر |
|---|---|
| `hr/ReportsPage.tsx` | 146 |
| `hr/HRCommunicationPage.tsx` | 121 |


### الكبيرة — أولوية بالأثر

| الصفحة | أسطر | ملاحظة مُقاسة |
|---|---|---|
| ~~`TrainingReportsPage`~~ | 761 | ✅ أُنجزت في `0351` — 5 أعمدة وهمية + انحدار 0327 |
| ~~`HRMovementAnalyticsPage`~~ | 715 | ✅ أُنجزت في `0350` — **تسريب بين المستأجرين** سُدّ |
| ~~`HRDashboard`~~ | 682 | ✅ أُنجزت في `0345` |
| ~~`PayrollPage`~~ | 660 | ✅ أُنجزت في `0348` |
| ~~`TrainingManagementPage`~~ | 623 | ✅ أُنجزت في `0352` — الإنشاء والتعديل كانا معطّلين تماماً |
| ~~`AnalyticsPage`~~ | 619 | ✅ أُنجزت في `0349` — 8 أعطال · 4 أرقام مُختلَقة أُزيلت |
| ~~`KioskPage`~~ | 609 | ✅ أُنجزت في `0347` |
| ~~`TalentMarketPage`~~ | 467 | ✅ أُنجزت في `0354` — **تسريب سِيَر الموظفين** سُدّ |
| `LoansPage` | 437 | — |

### الصغيرة — تحتاج فحص «ناقصة أم بسيطة؟»

```
AttendancePage        145 سطر   ← 3 استدعاءات SDK لوظيفة كبيرة
ReportsPage           146
HealthSafetyPage      153
ShiftSchedulingPage   193
HRServiceCenterPage   198
HRCommunicationPage   121   ← يلمس Supabase
```

`AttendancePage` بـ145 سطراً لإدارة حضور شركة كاملة رقمٌ يستدعي السؤال.

---

## رابعاً: ستّ صفحات تلمس Supabase مباشرة

خرقٌ لقاعدة «الصفحات لا تلمس Supabase — كل شيء عبر SDK»:

```
employee/AttendancePage.tsx        382
employee/NewProblemPage.tsx        495
employee/ProblemDetail.tsx         400
employee/SOPsPage.tsx              869
hr/HRCommunicationPage.tsx         121
hr/HRMovementAnalyticsPage.tsx     715
```

`sdk:boundary-check` يُبلّغ **52 انتهاكاً موروثاً** في 15 ملفاً (معظمها
في المالية والمخزون خارج نطاق هذه الخطة) — **صفر في كل ما كُتب حديثاً**.

---

## خامساً: بنود مفتوحة من الجولات السابقة

| البند | الحالة المُقاسة |
|---|---|
| `permissionService` (جدول `permissions`) | صار يُملأ من القاعدة في `0340` — **لا شاشة** تعرض الزمنيات المنفَّذة ولا تسجّل `actual_out_time`/`actual_return_time` |
| `corrective_actions` | مرتبط بالبلاغات بـFK — **بلا واجهة** |
| `biometric_devices_health` | جاهزة ومُختبَرة — **بلا واجهة** |
| `approval_rules` | فارغ — لا يمسّ HR (تستعمل `resolve_department_chain`) لكن يمسّ الوحدات الأخرى |
| `hr_approval_steps` | حذفها النهائي مؤجَّل حتى انتقال كل الشاشات إلى `unified_approval_steps` |
| نشر `admin-create-user` + `CRON_SECRET` | لم يُنفَّذ |
| `docs/e-procurement/GAP_COMPLETION_ROUND2_2026-08-04.md` | محذوف في git — يُحتمل أنه إعادة تسمية |

---

## سادساً: ما ليس مشكلة — نوثّقه لئلا يُعاد فحصه

- **`as any`**: صفر في البوابتين. الـ14 مطابقة كلها **تعليقات توثيقية**
  تصف ما أُزيل.
- **أزرار بلا معالج** (`onClick={() => {}}`): صفر.
- **`PlannedFeature` / placeholder**: صفر في البوابتين.
- **`TODO`/`FIXME`**: واحد فقط، في ملف مؤرشف
  (`hr/_archive/MovementAnalysisPage.tsx`).
- **قيود `incidents`** الثلاثة (`status`·`severity`·`category`) موجودة —
  صحّحتُ ادّعاءً أوّلياً بأنها غائبة.

---

## الحالة الصحّية الحالية

```
270 مايجريشن من الصفر           صفر فشل
48/48 ملف اختبار SQL             صفر إخفاق
184 تأكيد RLS في 10 سكربتات      صفر إخفاق
3268/3268 اختباراً في 136 ملفاً
tsc --noEmit                    EXIT=0
npm run build                   ✅
npm run lint                    0 خطأ · 1229 تحذيراً
npm run db:contract-check       PASS
npm run sdk:boundary-check      52 موروثاً — صفر في الجديد
```

---

## التسلسل المقترح

1. **`db push` لـ`0332`–`0341`** ← يسبق كل شيء
2. **جولة متصفح** على الشاشات الأربع الجديدة — أول اختبار بشري
3. **إكمال المرحلة 2**: `SOPsPage` (869 · الأكبر · يلمس Supabase) ثم
   `NewProblemPage`/`ProblemDetail` (يكملان دورة البلاغات التي بدأناها)
4. **بدء المرحلة 4**: `HRDashboard` — الواجهة الأولى للموارد
5. المرحلتان 5 و6

---

## ★ بند مفتوح من جولة 0360 — فشلٌ عابر غير مُشخَّص

في أول تشغيل شامل على قاعدة `D86`، فشل
`tools/dev/verify-bonus-lifecycle-0358-rls.sh` بفحصٍ **واحد**
(`❌ 0358 RLS — 1 فشلاً`) ولم يُلتقَط سطر الفشل نفسه.

**ما جُرّب للتكرار — 145 تشغيلاً بلا نجاح:**

| المحاولة | النتيجة |
|---|---|
| تشغيله منفرداً على `D86` × 2 | ✅ صفر فشل |
| 3 جولات كاملة × 29 سكربتاً على `D86` | ✅ صفر فشل |
| قاعدتان نظيفتان (`D88` · `D89`) × 29 | ✅ صفر فشل |
| فحص عيّنة 0358 العالقة في `D86` | صفر صفّ — التنظيف سليم |

**لم يُشخَّص السبب.** الفرض الأرجح تداخلٌ زمنيّ بين سكربتين على
القاعدة نفسها — **فرضٌ لا إثبات**. اختبارٌ يفشل مرّةً من 145 هو اختبار
غير موثوق بحدّ ذاته، ويستحق حارساً يطبع السطر الفاشل دائماً.

**الخطوة المقترحة:** إضافة `set -x` مشروط أو حفظ سجلّ كل سكربت في ملف
مستقلّ ضمن التشغيل الشامل (فُعِّل جزئياً في `/tmp/rls-logs/`).


---

## ★ بند جديد من جولة 0361 — `succession_development_plans`

جدولٌ حيٌّ في القاعدة بـ**صفر صفّ**، و`successionDevelopmentPlanService`
مُصدَّرة من `src/services/sdk/index.ts` **ولا يستوردها أيّ ملف** (مسحُ
المستودع كلّه).

الأثر: «الفجوات» تُكتب نصّاً حراً في `succession_candidates.gaps` ولا
تتحوّل إلى **خطة عمل** — لا تدريب ولا إرشاد ولا تكليف ولا شهادة.
وهذا نصف قيمة تخطيط التعاقب.

**قرارٌ يحتاج المستخدم:** أنُفعّله في جولةٍ لاحقة (ربطه بـ
`TrainingManagement` منطقيّ) أم نُسقط الجدول والخدمة معاً؟


---

## ★ بند جديد من جولة 0362 — الموظَّف الجديد بلا حساب دخول

`application_hire` تُنشئ صفّ `employees` كاملاً (بريد · هاتف · قسم ·
رمز) وتربطه بالطلب — لكن **بلا `user_id`**، فالموظّف الجديد لا يملك
حساب دخول إلى المنصّة.

الربط يحتاج صفّاً في `auth.users` وهو **خارج نطاق المايجريشن** (يُنشأ
عبر Supabase Auth بدعوةٍ بالبريد أو من «إدارة المستخدمين»).

**قرارٌ يحتاج المستخدم:** أنُضيف دعوةً بالبريد في جولةٍ لاحقة (تستدعي
Edge Function) أم يبقى إنشاء الحساب يدوياً من «إدارة المستخدمين» ثم
يربطه محفّز `tg_ensure_employee_row` بالقسم؟


---

## ~~★★★ بند جولة 0363 — `sync_hr_source_status` بلا سبب~~ ✅ أُغلق في `0366`

> **★ أُغلق في المايجريشن `0366`** — «قد يكون» صارت **مُثبتة**:
> المسبار أظهر `status = rejected · rejection_reason = <NULL>`
> والتعليل سليمٌ في `hr_approval_steps.comments`.
> العلاج: `loan_apply_rejection_reason()` + `tg_loan_sync_reason` +
> `chk_employee_loans_rejection_reason` (القيد الذي كان غيابُه سببَ
> استتار العطل). **14 تأكيداً · 10/10 عكساً.**
> التفصيل في `docs/DISCIPLINARY_0365.md` — ملحق 0366.

### النصّ الأصليّ للبند (للسجلّ)


`sync_hr_source_status(p_request_id UUID, p_final TEXT)` — **توقيعٌ بلا
سبب** يخدم `expense` و`loan` و`leave`. حين يرفض المستخدم عبر صندوق
الموافقات، `unified_approval_decide(…, 'rejected', 'التعليل')` تُخزّن
السبب في `hr_approval_steps.comments`، ثم المزامنة تكتب
`status='rejected'` في الجدول المصدر **بلا نقل السبب**.

**أثره على النفقات عولج في 0363** بمحفّز `tg_expense_sync_reason`
يستخرج التعليق من آخر خطوة رافضة.

**★ لكن `employee_loans` قد يكون فيه العطل نفسه** — رفضٌ عبر الصندوق
بلا تعليل. لم يُفحص بعد لأن لا قيد يكشفه هناك.

~~**الخطوة المقترحة:** فحص فرع `loan` في `sync_hr_source_status` بمسبار،
وإن ثبت العطل يُعالَج بالنمط نفسه (محفّز + دالة استخراج).~~
**✅ نُفِّذت في `0366` — العطل ثبت وعولج.**


---

## ★ بند جديد من جولة 0364 — عمودا الربط بلا مفاتيح

أُضيف `job_application_id` و`offboarding_id` إلى `employee_contracts`
(العطل ⑬: العقد جزيرةٌ معزولة) — لكن **بلا مفاتيح أجنبية ولا ملء
تلقائيّ**. الربط الفعليّ يحتاج تعديل دالّتين قائمتين:

· `application_hire` (0362) — تُنشئ صفّ `employees` ولا تُنشئ له عقداً
· `offboarding_finalize` (0359) — تُنهي الخدمة ولا تُنهي العقد

**قرارٌ يحتاج المستخدم:** أنربطهما في جولةٍ لاحقة (يمسّ مايجريشنَين
سابقَين) أم يبقى العقد يُنشأ ويُنهى يدوياً من صفحة العقود؟
