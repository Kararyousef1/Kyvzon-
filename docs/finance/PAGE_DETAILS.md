═══════════════════════════════════════════════════════════════
         تفاصيل تصميم صفحات بوابة المالية (تفصيلي جداً)
         لكل صفحة: SDK Services + Design + Special Calls + Integration
═══════════════════════════════════════════════════════════════

📁 الملف المرجعي: /home/user/kyvzon_repo/src/pages/app/finance/
📁 كل صفحة: 29 سطر (أساسية) أو أكثر (Dashboard: 115 سطر)
📁 كل صفحة متصلة بـ SDK عبر useEffect بدون بيانات وهمية

═══════════════════════════════════════════════════════════════
1. FinancialDashboard.tsx (115 سطر — الأكثر تعقيداً)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • generalLedgerService.findByStatus('posted') — قيود منشورة
  • (مستقبلاً) budgetService.getTotalThisMonth() — ميزانية الشهر
  • (مستقبلاً) cashManagementService.getBalance() — النقد المتاح

📊 مكونات التصميم الخاصة:
  • 3 بطاقات KPIs (مدين / دائن / عدد القيود) — ألوان متدرجة
  • جدول آخر 5 قيود منشورة — متصل بـ SDK
  • حالة تحميل (Loading Spinner ضمني عبر setLoading)
  • زر "إضافة قيد جديد" → ينتقل لـ JournalEntriesPage

🔗 التكامل مع النظام:
  • PayrollService (عبر PayrollGLIntegration) → يسجل قيوداً في GL
  • FinanceService (عبر التكامل المستقبلي) → يسجل مصروفات في GL

📋 التفصيلة الصغيرة المهمة:
  • totalDebit و totalCredit يُحسبان عبر reduce() من البيانات الفعلية
  • لا توجد أي قيمة ثابتة — كل الأرقام من SDK

═══════════════════════════════════════════════════════════════
2. GeneralLedgerPage.tsx (115 سطر — متكامل)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • generalLedgerService.findByStatus('posted') — البيانات الأولية
  • (عبر الفلترة) كل الحالات: draft, posted, reversed

📊 مكونات التصميم الخاصة:
  • 3 بطاقات ملخصة (مدين / دائن / عدد) — ألوان متدرجة
  • جدول كامل بـ 6 أعمدة (رقم القيد، تاريخ، وصف، مدين، دائن، حالة)
  • فلترة حسب الحالة (كل / مسودة / منشور)
  • بحث نصي (برقم القيد أو الوصف أو المرجع)
  • زر "إضافة قيد جديد"
  • مبدأ Immutability مذكور بوضوح في الأسفل

📋 التفصيلة الصغيرة المهمة:
  • كل صف في الجدول يعرض حالة القيد بألوان مختلفة (أخضر=منشور، أصفر=مسودة)
  • لا يمكن تعديل القيد من هذه الصفحة — فقط عرض
  • البيانات تأتي من SDK مباشرة (لا مصفوفات وهمية)

═══════════════════════════════════════════════════════════════
3. JournalEntriesPage.tsx (هيكل أساسي — 4 سطور حالياً)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة (المستقبلية):
  • generalLedgerService.createJournal()
  • journalEntryService.addLine()
  • chartOfAccountService.findActive() — لاختيار الحسابات

📊 مكونات التصميم المفقودة (من الخطة):
  • نموذج إنشاء قيد جديد (Form)
  • قسم بنود القيد (Dynamic List) — إضافة/حذف بنود
  • تحقق لحظي: مجموع المدين = مجموع الدائن
  • زر "حفظ كمسودة" أو "نشر"
  • زر "عكس" (Reverse) — ينشئ قيداً عكسياً جديداً

📋 التفصيلة الصغيرة المهمة:
  • القيد يجب أن يكون متوازناً قبل الإرسال (CONSTRAINT balanced_check في SQL)
  • لا يُسمح بتعديل القيد بعد النشر (Immutability)
  • كل بند يحتاج account_id من chart_of_accounts

═══════════════════════════════════════════════════════════════
4. ChartOfAccountsPage.tsx (29 سطر — جدول حسابات)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • chartOfAccountService.findActive() — الحسابات النشطة
  • chartOfAccountService.findByType('Asset') — حسب النوع

📊 مكونات التصميم الخاصة:
  • عرض شجري (Tree) للحسابات (مستقبلي — حالياً جدول مسطح)
  • زر "إضافة حساب جديد"
  • بحث في الكود أو الاسم
  • عرض نوع الحساب بلون مختلف (أخضر=أصول، أحمر=التزامات، أزرق=إيرادات)

📋 التفصيلة الصغيرة المهمة:
  • كل حساب له parent_id (هيكل هرمي من 4 مستويات حسب البحث)
  • الكود يجب أن يكون فريداً داخل الشركة (UNIQUE constraint في SQL)
  • لا يمكن حذف حساب إذا كان مرتبطاً بقيود يومية (ON DELETE RESTRICT)

═══════════════════════════════════════════════════════════════
5. AccountsPayablePage.tsx (29 سطر — جدول فواتير)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • accountsPayableService.findAll()
  • accountsPayableService.approveInvoice()
  • accountsPayableService.payInvoice()

📊 مكونات التصميم الخاصة:
  • جدول فواتير الموردين مع فلترة حسب الحالة
  • زر "موافقة" (Draft → Approved)
  • زر "دفع" (Approved → Paid) — يُنشئ قيداً في GL تلقائياً
  • زر "تقرير الشيخوخة" (Aging Report)

📋 التفصيلة الصغيرة المهمة:
  • عند الدفع، يُنشئ قيداً في journal_entries تلقائياً (التكامل مع GL)
  • المطابقة الثلاثية (PO + Receipt + Invoice) — مذكورة في البحث العالمي

═══════════════════════════════════════════════════════════════
6. AccountsReceivablePage.tsx (29 سطر — جدول فواتير العملاء)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • accountsReceivableService.findAll()
  • accountsReceivableService.collectPayment()

📊 مكونات التصميم الخاصة:
  • جدول فواتير العملاء
  • زر "تحصيل دفعة" — يسجل دفعة جزئية أو كاملة
  • تقارير التحصيل (Collection Reports)

📋 التفصيلة الصغيرة المهمة:
  • كل دفعة تُنشئ قيداً في GL (Debit Cash / Credit AR)
  • إدارة المخاطر الائتمانية (Credit Control) — مذكورة في البحث

═══════════════════════════════════════════════════════════════
7. CashManagementPage.tsx (29 سطر — إدارة النقدية)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • cashManagementService.findAll()
  • bankReconciliationService (مستقبلي)

📊 مكونات التصميم الخاصة:
  • بطاقات أرصدة البنوك (Bank Balances)
  • زر "مصالحة" لكل بنك
  • تدفق نقدي متوقع (Cash Flow Forecast)

📋 التفصيلة الصغيرة المهمة:
  • المصالحة البنكية تُسجل في bank_reconciliations مع statement_balance و adjusted_balance
  • الفرق (difference) يجب أن يكون صفراً بعد المصالحة

═══════════════════════════════════════════════════════════════
8. TaxManagementPage.tsx (29 سطر — إدارة الضرائب)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • taxService.findAll()
  • taxService.calculateTax()

📊 مكونات التصميم الخاصة:
  • جدول أكواد الضرائب (VAT/GST)
  • زر "إضافة كود ضريبة"
  • عرض معدل الضريبة لكل كود
  • حساب تلقائي على المعاملات

📋 التفصيلة الصغيرة المهمة:
  • كل معاملة مالية يمكن ربطها بـ tax_code عبر journal_entry_lines
  • الإقرارات الضريبية تُولد تلقائياً من القيود المرتبطة

═══════════════════════════════════════════════════════════════
9. BudgetPage.tsx (29 سطر — الميزانيات)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • budgetService.findAll()
  • budgetService.compareActualVsBudget()

📊 مكونات التصميم الخاصة:
  • اختيار سنة مالية
  • جدول ميزانية: الحسابات مع المبالغ المخططة
  • مقارنة الفعلي مقابل المخطط
  • زر "تصدير"

📋 التفصيلة الصغيرة المهمة:
  • variance = budget_amount - actual_amount (من journal_entries)
  • المقارنة تتم لحظياً عند الضغط على زر المقارنة

═══════════════════════════════════════════════════════════════
10. FinancialReportsPage.tsx (29 سطر — التقارير المالية)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • financialReportService.generatePAndL()
  • financialReportService.generateBalanceSheet()
  • financialReportService.generateCashFlow()

📊 مكونات التصميم الخاصة:
  • اختيار نوع التقرير (P&L, Balance Sheet, Cash Flow)
  • اختيار الفترة
  • عرض التقرير في جدول + رسوم بيانية
  • زر "توليد" + "تصدير PDF"

📋 التفصيلة الصغيرة المهمة:
  • P&L: Revenue (من chart_of_accounts نوع Revenue) - Expenses (نوع Expense)
  • Balance Sheet: Assets = Liabilities + Equity (من chart_of_accounts)
  • Cash Flow: من Cash + GL (من journal_entries المرتبطة بـ Cash)

═══════════════════════════════════════════════════════════════
11. SystemNotesPage.tsx (جديد — من البحث العميق)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • systemNoteService.findByEntity()
  • systemNoteService.createNote()

📊 مكونات التصميم الخاصة:
  • عرض الملاحظات حسب الكيان (جدول، قيد، فاتورة)
  • زر "إضافة ملاحظة"
  • لا يمكن التعديل أو الحذف (Immutability)

📋 التفصيلة الصغيرة المهمة:
  • كل ملاحظة تُسجل مع user_id + timestamp
  • لا يوجد زر تعديل أو حذف
  • مبدأ Square Books (Append-Only Ledger)

═══════════════════════════════════════════════════════════════
12. ApprovalsPage.tsx (جديد — من البحث العميق)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • approvalService.findPending()
  • approvalService.approve()
  • approvalService.reject()

📊 مكونات التصميم الخاصة:
  • قائمة الموافقات المعلقة
  • فلترة حسب الحالة (معلقة / موافق / مرفوض)
  • أزرار "موافقة" و"رفض" لكل طلب

📋 التفصيلة الصغيرة المهمة:
  • كل طلب موافقة يحتوي على reference_id (رابط القيد أو الفاتورة)
  • كل خطوة موافقة تُسجل مع approver_role + approver_id + timestamp
  • لا يمكن الموافقة على قيد غير موجود أو غير صالح

═══════════════════════════════════════════════════════════════
13. RevenueRecognitionPage.tsx (جديد — من البحث العميق)
═══════════════════════════════════════════════════════════════

📡 SDK Services المستدعاة:
  • revenueRecognitionService.findActive()
  • revenueRecognitionService.createContract()
  • revenueRecognitionService.completeContract()

📊 مكونات التصميم الخاصة:
  • جدول العقود
  • زر "إضافة عقد جديد"
  • عرض طريقة الاعتراف (مستقيم / مرحلة / استخدام)
  • زر "إكمال" (Complete Contract)

📋 التفصيلة الصغيرة المهمة:
  • كل عقد يرتبط بـ customer_name
  • طريقة الاعتراف تُحدد كيف يُسجل الإيراد (خط مستقيم = كل شهر نفس المبلغ)
  • جداول recognition_schedules تُسجل الإيرادات الشهرية تلقائياً
