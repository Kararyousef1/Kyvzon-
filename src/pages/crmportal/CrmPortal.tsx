/**
 * ═════════════════════════════════════════════════════════════════════════
 *  CrmPortal — غلاف بوابة CRM
 *
 *  يُعرَض داخل AppLayout (الذي يوفّر الشريط الجانبي المخصّص CrmSidebar
 *  والهيدر والحاوية المتجاوبة). غلاف بسيط للمسارات الفرعية.
 *  الوحدات الست تُضاف تدريجياً كمسارات فرعية.
 * ═════════════════════════════════════════════════════════════════════════
 */
import { Outlet } from 'react-router-dom';

export default function CrmPortal() {
  return <Outlet />;
}
