/**
 * ═════════════════════════════════════════════════════════════════════════
 *  MarketingPortal — غلاف بوابة التسويق
 *
 *  يُعرَض داخل AppLayout (الذي يوفّر الشريط الجانبي المخصّص MarketingSidebar
 *  والهيدر والحاوية المتجاوبة). لذا هذا المكوّن غلاف بسيط للمسارات الفرعية.
 *  الوحدات السبع تُضاف تدريجياً كمسارات فرعية.
 * ═════════════════════════════════════════════════════════════════════════
 */
import { Outlet } from 'react-router-dom';

export default function MarketingPortal() {
  return <Outlet />;
}
